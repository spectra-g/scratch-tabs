export const registerJsonProvider = (monaco: any) => {
  // JSON formatting is built into Monaco, so we don't need to register a custom provider
  // This file exists for consistency and future customization if needed
};